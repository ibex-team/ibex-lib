language: cpp

compiler:
  - gcc
  - clang

before_script:
  - sudo apt-get update -q
  - sudo apt-get install -y libcpptest-dev
  - mkdir build
  - cd build
  - cmake ..

script: 
  - make
  - make test
  - sudo make install
